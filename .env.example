# API Keys
NEWS_API_KEY=
GEMINI_API_KEY=

# Email Settings
# Choose your email sender type: 'smtp' or 'ncloud'
EMAIL_SENDER_TYPE=smtp

# SMTP Settings (if EMAIL_SENDER_TYPE is 'smtp')
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=

# Naver Cloud Outbound Mailer Settings (if EMAIL_SENDER_TYPE is 'ncloud')
NCLOUD_ACCESS_KEY=
NCLOUD_SECRET_KEY=
NCLOUD_SENDER_ADDRESS=

# Common Email Settings
# A JSON string representing a list of recipients.
# Each recipient is an object with "email" and an optional "template".
# Example: [{"email": "user1@example.com"}, {"email": "user2@example.com", "template": "custom_template.html"}]
RECIPIENTS=[]
SENDER_NAME=AI News Feeder
DEFAULT_EMAIL_TEMPLATE=email_template.html

# News Settings
NEWS_ARTICLE_COUNT=5